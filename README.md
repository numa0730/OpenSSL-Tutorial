# OpenSSL-Tutorial
## OpenSSL Sample Code
English text is below.  
* OpenSSLを使ったハッシュ値の計算 (/src/Hash)  
	* ハッシュ関数SHA-256とは  
	* ハッシュ関数と共に利用されるSaltとは  
* OpenSSLを使った暗号鍵の作成 (/src/GenPrivateKey)  
	* パスワードから暗号鍵を作る  
	* 暗号鍵を自動的に作成する  
* OpenSSLを使った鍵ペアの生成 (/src/GenKeypair) ※作成中  
* 暗号鍵を安全に保存する手段 (/src/pkcs12) ※作成中  
* OpenSSLを使った暗号文の生成 (/src/Encrypt) ※作成中  
* OpenSSLを使った電子署名 (/src/Sign) ※作成中  

* Hash Calculation by OpenSSL (/src/Hash)  
	* What is SHA-256 ?  
	* What is salt to be used with hash
* Private Key generation by OpenSSL (/src/GenPrivateKey)  
	* Derives private key from password
	* Generate private key from random
* Generate key pair by OpenSSL(/src/GenKeypair) ... not yet      
* Secure way to store private key (/src/pkcs12) ... not yet  
* Encrypt plain text by OpenSSLを(/src/Encrypt) ... not yet  
* Sign document by OpenSSL (/src/Sign) ... not yet  
  
解説 https://numajiri.blog/